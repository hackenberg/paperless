# paperless

```bash
$ git clone https://github.com:hackenberg/paperless.git
$ cd paperless
$ python -m venv ./venv
$ pip install -e .
```

```bash
$ export FLASK_APP=paperless
$ flask init-db
$ flask run
```
